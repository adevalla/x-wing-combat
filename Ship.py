class Ship:

    def __init__(self, data):
        """
        Want a master spreadsheet of ship stats. external generator function (or internal one, I guess)
        to generate ship based on spreadsheet row. Idk what the best of instantiating it is."""
        # Primary Weapon attack (ignore secondaries for now)
        # agility
        # hull
        # shield


def generateShip(data):
    return Ship(data)
